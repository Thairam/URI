#include <bits/stdc++.h>

using namespace std;

int main (){
	int x, y, n, id, a, l, c;
	
	scanf("%d", &n);
	
	while(n--){
		scanf("%d %d", &x, &y);
		for(int i=0; i < x; i++){
			scanf("%d %d %d %d", &id, &a, &l, &c);
			
		}		
	}
	
	return 0;
}
