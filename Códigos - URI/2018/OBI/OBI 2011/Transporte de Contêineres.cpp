#include <bits/stdc++.h>

using namespace std;

int main(){
	
	int a, b, c;
	int x, y, z;
	
	cin >> a >> b >> c;
	cin >> x >> y >> z;
	
	cout << ( (x / a) * (y / b) * (z / c) )	<< endl;
	
	return 0;
}